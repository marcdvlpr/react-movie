import React from 'react';
import HeroImage from '../HeroImage';

const Home = (props) => {
  return (
    <>
      <HeroImage
        image={''}
        title={''}
        text={''}
      />
    </>
  )
};

export default Home;
