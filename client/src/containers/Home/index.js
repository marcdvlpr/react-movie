import React from 'react';
import HeroImage from '../HeroImage';
import { POSTER_SIZE, BACKDROP_SIZE } from '../../config';

const Home = (props) => {
  return (
    <>
      <HeroImage
        image={''}
        title={''}
        text={''}
      />
    </>
  )
};

export default Home;
